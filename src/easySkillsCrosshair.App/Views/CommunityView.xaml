<UserControl x:Class="easySkillsCrosshair.App.Views.CommunityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:easySkillsCrosshair.App.ViewModels"
             xmlns:controls="clr-namespace:easySkillsCrosshair.App.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CommunityViewModel}"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Community-Fadenkreuze" Style="{StaticResource Heading1}" Margin="0,0,0,4" />
            <TextBlock Text="Von der Community geteilte Konfigurationen entdecken (Demo-Daten, noch nicht online)."
                       Style="{StaticResource Text.Muted}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Style="{StaticResource TextBox.Dark}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Margin="12,0,0,0" Style="{StaticResource Button.Primary}"
                    Content="Suchen" Command="{Binding SearchCommand}" />
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      Padding="0,0,10,0">
        <ItemsControl ItemsSource="{Binding Listings}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" Width="220" Margin="0,0,16,16">
                        <StackPanel>
                            <controls:CrosshairPreviewControl Profile="{Binding Profile}" Height="130" Margin="0,0,0,12" />
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource Heading2}" HorizontalAlignment="Left" />
                                <TextBlock Text="{Binding Likes, StringFormat='♥ {0}'}" Style="{StaticResource Text.Muted}"
                                           HorizontalAlignment="Right" Foreground="{StaticResource Brush.Gold}" />
                            </Grid>
                            <TextBlock Text="{Binding AuthorName, StringFormat='von {0}'}" Style="{StaticResource Text.Muted}" Margin="0,0,0,12" />
                            <Button Content="Jetzt verwenden" Style="{StaticResource Button.Primary}"
                                    Command="{Binding DataContext.UseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
