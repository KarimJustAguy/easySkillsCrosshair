<UserControl x:Class="easySkillsCrosshair.App.Views.DpiCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:easySkillsCrosshair.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DpiCalculatorViewModel}"
             Background="{StaticResource Brush.Background}">
    <Grid Margin="28">
        <StackPanel MaxWidth="420" HorizontalAlignment="Left">
            <TextBlock Text="DPI &amp; Sensitivity Calculator" Style="{StaticResource Heading1}" Margin="0,0,0,4" />
            <TextBlock Text="Uneingeschränkt nutzbar, auch im Trial." Style="{StaticResource Text.Muted}" Margin="0,0,0,20" />

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="SPIEL" Style="{StaticResource Label.Section}" />
                    <ComboBox Style="{StaticResource ComboBox.Gold}" ItemsSource="{Binding Presets}"
                              SelectedItem="{Binding SelectedPreset}" DisplayMemberPath="Name" Margin="0,0,0,20" />

                    <TextBlock Text="{Binding Dpi, StringFormat='Maus-DPI: {0:0}'}" Style="{StaticResource Text.Muted}" />
                    <Slider Style="{StaticResource Slider.Gold}" Minimum="100" Maximum="26000" Value="{Binding Dpi}" Margin="0,0,0,16" />

                    <TextBlock Text="{Binding Sensitivity, StringFormat='In-Game-Sensitivity: {0:0.###}'}" Style="{StaticResource Text.Muted}" />
                    <Slider Style="{StaticResource Slider.Gold}" Minimum="0.01" Maximum="10" Value="{Binding Sensitivity}" Margin="0,0,0,16" />

                    <StackPanel Visibility="{Binding IsCustomYawEditable, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="{Binding CustomYaw, StringFormat='Yaw pro Count: {0:0.####}'}" Style="{StaticResource Text.Muted}" />
                        <Slider Style="{StaticResource Slider.Gold}" Minimum="0.001" Maximum="0.3" Value="{Binding CustomYaw}" Margin="0,0,0,16" />
                    </StackPanel>

                    <Separator Margin="0,4,0,16" Background="{StaticResource Brush.Border}" />

                    <TextBlock Text="EDPI" Style="{StaticResource Label.Section}" />
                    <TextBlock Text="{Binding EffectiveDpi, StringFormat='{}{0:0.#}'}" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource Brush.Gold}" Margin="0,0,0,16" />

                    <TextBlock Text="360°-DISTANZ" Style="{StaticResource Label.Section}" />
                    <TextBlock Text="{Binding CmPer360, StringFormat='{}{0:0.##} cm'}" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource Brush.Gold}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
