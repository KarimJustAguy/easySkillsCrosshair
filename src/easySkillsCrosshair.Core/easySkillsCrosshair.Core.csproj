<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>easySkillsCrosshair.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Built-in fallback; the App also ships an editable copy as Data\games.json. -->
    <EmbeddedResource Include="Sensitivity\games.json" LogicalName="easySkillsCrosshair.Core.Sensitivity.games.json" />
  </ItemGroup>

</Project>
